<body>
 <h1>Editing Book</h1>


  <% if @book.errors.any? %>
  <div id="error_explanation">
  <h2><%= @book.errors.count %> errors prohibited this book from being saved:</h2>
   <ul>
    <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
   </ul>
  </div>

  <div class="field">
    <label for="book_title">Title</label>
    <div class="field_with_errors">
     <%= f.text_field :title %>
    </div>

    <div class="field">
    <label for="book_title">Body</label>
    <div class="field_with_errors">
     <%= f.text_field :body %>
    </div>

    <div class="actions">
    <p><%= f.submit 'Create Book' %></p>
    </div>

    <% end %>

  <%= form_with model: @book do |f| %>
    <div class="field">
    <label for="book_title">T<h4>Title</h4></label>
     <%= f.text_field :title %>
   </div>

   <div class="field">
    <label for="book_title"><h4>Body</h4></label>
     <%= f.text_area :body %>
   </div>

   <div class="actions">
     <%= f.submit 'Update Book' %></p>
   </div>

  <%= link_to "Edit", edit_book_path(@book.id) %>
   |
  <%= link_to "Back", book_path(book.id) %>

 <% end %>

</body>